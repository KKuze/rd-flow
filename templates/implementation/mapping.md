# Spec → code mapping — {{spec_name}}

| Spec artefact                | Code location          | Notes |
| ---------------------------- | ---------------------- | ----- |
| `tla/model.tla::Inv`         | (file:lineno)          |       |
| `dafny/spec.dfy::Step`       | (file:lineno)          |       |

Update this table whenever a verified obligation is realised in code. The
validate phase uses it as the checklist for "is the implementation
faithful to the spec?"
